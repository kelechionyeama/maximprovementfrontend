const appInfo = { version: "1.0.0", build: 1 };

const auth = ""

const BASE_URL = "http://10.101.196.8:3333/api";

const isTestFlightBuild = false;

export { appInfo, BASE_URL, auth, isTestFlightBuild };